# P-Gen

# Khởi động backend :

ở thư mục backend chạy lệnh : node src/setupDatabase.js
Lưu ý : chỉ chạy 1 lần duy nhất

Tiếp theo chạy : npm run dev
Server sẽ chạy trên cổng 5001 đồng thời tự động kết nối với db đã tạo qua src/config/db.js
